# Тестирование Push-уведомлений

## Шаг 1: Установите APK на телефон

APK находится здесь: `android/app/build/outputs/apk/debug/app-debug.apk`

Скопируйте файл на телефон и установите (разрешите установку из неизвестных источников).

## Шаг 2: Запустите приложение

1. Откройте приложение
2. Авторизуйтесь как пользователь
3. На главном экране нажмите кнопку **"Токен"** (иконка Copy)
4. FCM токен будет скопирован в буфер обмена
5. Отправьте токен себе (например, через Telegram/WhatsApp)

## Шаг 3: Убедитесь что сервер запущен

На компьютере проверьте что сервер работает:

```bash
# Если сервер не запущен, запустите:
cd e:\tehnoprokat\server
node push-server.js
```

Сервер должен показать: `Push notification server running on port 3002`

## Шаг 4: Отправьте тестовое уведомление

### Вариант А: Через скрипт

```bash
cd e:\tehnoprokat\server
node test-notification.js ВАШ_FCM_ТОКЕН
```

### Вариант Б: Через curl

```bash
curl -X POST http://localhost:3002/api/send-notification ^
  -H "Content-Type: application/json" ^
  -d "{\"token\":\"ВАШ_FCM_ТОКЕН\",\"title\":\"Тест\",\"body\":\"Проверка уведомлений\"}"
```

### Вариант В: Через Postman

1. Откройте Postman
2. Создайте POST запрос: `http://localhost:3002/api/send-notification`
3. Headers: `Content-Type: application/json`
4. Body (raw JSON):
```json
{
  "token": "ВАШ_FCM_ТОКЕН",
  "title": "🎉 Тестовое уведомление",
  "body": "Push-уведомления работают!",
  "data": {
    "test": "true"
  }
}
```

## Шаг 5: Проверьте уведомление

1. **Приложение открыто**: Уведомление появится внутри приложения
2. **Приложение свернуто**: Уведомление появится в шторке уведомлений
3. **Приложение закрыто**: Уведомление появится в шторке уведомлений

## Проблемы?

### Токен не копируется
- Подождите 5-10 секунд после запуска приложения
- Проверьте логи в Android Studio (Logcat)

### Уведомление не приходит
1. Проверьте что сервер запущен
2. Проверьте что токен правильный
3. Проверьте интернет на телефоне
4. Проверьте что Firebase настроен правильно

### Проверка логов
```bash
# В Android Studio откройте Logcat и фильтруйте по:
# - "Push"
# - "FCM"
# - "Notification"
```

## Автоматические уведомления

После настройки, уведомления будут приходить автоматически когда:
- Водитель принял заказ
- Водитель выехал
- Заказ выполнен
- Заказ отменен
