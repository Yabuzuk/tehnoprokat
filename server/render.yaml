services:
  - type: web
    name: push-notification-server
    env: node
    buildCommand: npm install
    startCommand: node push-server.js
    envVars:
      - key: NODE_ENV
        value: production
