# 🚀 Руководство по деплою

## Подготовка к деплою

### 1. Настройка Supabase

1. Создайте проект в [Supabase](https://supabase.com)
2. Выполните SQL из `supabase-schema.sql` в SQL Editor
3. Настройте RLS политики (уже включены в схему)
4. Получите URL и anon key из Settings > API

### 2. Переменные окружения

Создайте `.env` файл:
```bash
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

## Деплой на Vercel

### Автоматический деплой

1. Подключите репозиторий к Vercel
2. Установите переменные окружения в настройках проекта
3. Деплой произойдет автоматически при пуше в main

### Ручной деплой

```bash
npm install -g vercel
vercel login
vercel --prod
```

## Деплой на Netlify

### Через интерфейс

1. Подключите репозиторий
2. Build command: `npm run build`
3. Publish directory: `dist`
4. Установите переменные окружения

### Через CLI

```bash
npm install -g netlify-cli
netlify login
netlify deploy --prod --dir=dist
```

## Docker деплой

### Локальная сборка

```bash
docker build -t water-delivery-app .
docker run -p 3000:3000 water-delivery-app
```

### Docker Compose

```yaml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:80"
    environment:
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
```

## Настройка домена и SSL

### Vercel
- Домен настраивается автоматически
- SSL сертификат выдается автоматически

### Netlify
- Добавьте домен в настройках
- SSL настраивается автоматически

### Собственный сервер
```nginx
server {
    listen 443 ssl http2;
    server_name yourdomain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## PWA настройки

### Service Worker
- Автоматически генерируется Vite PWA плагином
- Кэширует статические ресурсы и API запросы

### Установка на устройства
- Android: "Добавить на главный экран"
- iOS: "Добавить на экран Домой"
- Desktop: Иконка установки в адресной строке

## Мониторинг и аналитика

### Supabase Analytics
- Встроенная аналитика в панели Supabase
- Мониторинг API запросов и производительности

### Vercel Analytics
```bash
npm install @vercel/analytics
```

```tsx
import { Analytics } from '@vercel/analytics/react'

function App() {
  return (
    <>
      <YourApp />
      <Analytics />
    </>
  )
}
```

### Google Analytics
```html
<!-- В index.html -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

## Оптимизация производительности

### Сжатие ресурсов
- Gzip/Brotli сжатие (настроено в nginx.conf)
- Минификация JS/CSS (автоматически в Vite)

### Кэширование
- Service Worker кэширует ресурсы
- CDN кэширование (Vercel/Netlify)
- Browser caching headers

### Lazy Loading
- Компоненты загружаются по требованию
- Изображения с lazy loading

## Безопасность

### HTTPS
- Обязательно используйте HTTPS в продакшене
- Настройте редирект с HTTP на HTTPS

### CSP Headers
```nginx
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'";
```

### Environment Variables
- Никогда не коммитьте .env файлы
- Используйте секреты в CI/CD

## Резервное копирование

### База данных
- Supabase автоматически создает бэкапы
- Настройте дополнительные бэкапы при необходимости

### Код
- Используйте Git для версионирования
- Настройте автоматические бэкапы репозитория

## Масштабирование

### Горизонтальное масштабирование
- Используйте CDN для статических ресурсов
- Load balancer для множественных инстансов

### Вертикальное масштабирование
- Увеличьте ресурсы сервера при необходимости
- Мониторьте использование CPU/RAM

## Troubleshooting

### Частые проблемы

1. **Ошибки CORS**
   - Проверьте настройки Supabase
   - Убедитесь, что домен добавлен в разрешенные

2. **Ошибки сборки**
   - Проверьте переменные окружения
   - Убедитесь в совместимости версий Node.js

3. **PWA не устанавливается**
   - Проверьте manifest.json
   - Убедитесь, что сайт работает по HTTPS

### Логи и отладка

```bash
# Vercel
vercel logs

# Netlify
netlify logs

# Docker
docker logs container-name
```