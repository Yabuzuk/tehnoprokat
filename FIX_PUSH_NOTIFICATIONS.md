# 🔧 ИСПРАВЛЕНИЕ PUSH-УВЕДОМЛЕНИЙ

## 🎯 Проблема
Тестовые уведомления работают, но реальные уведомления при изменении статуса заказа не приходят.

## ✅ Что исправлено

### 1. **Обновлена логика отправки уведомлений**
- Исправлен файл `src/hooks/useOrders.ts`
- Теперь используется прямая отправка через Firebase REST API
- Добавлен правильный API ключ из Firebase проекта

### 2. **Пересобран APK**
- Обновленный APK: `android/app/build/outputs/apk/debug/app-debug.apk`
- Размер: ~5.4 МБ
- Содержит исправленную логику уведомлений

## 🚀 Как протестировать

### Шаг 1: Установи обновленный APK
```bash
# Установи новый APK на устройство
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

### Шаг 2: Создай тестовый сценарий
1. **Пользователь**: Создай заказ
2. **Водитель**: Прими заказ
3. **Проверь**: Должно прийти уведомление пользователю

### Шаг 3: Проверь логи
Открой Chrome DevTools в приложении и смотри консоль:
```
📤 Отправка уведомления пользователю: [user_id]
📱 Отправка на токен: [token_prefix]...
✅ Уведомление отправлено через Firebase: [result]
```

## 🔍 Диагностика проблем

### Если уведомления все еще не приходят:

#### 1. **Проверь токен в базе данных**
```sql
-- В Supabase SQL Editor:
SELECT * FROM push_tokens WHERE user_id = '[user_id]';
```

#### 2. **Проверь Firebase ключ**
- Убедись что в `useOrders.ts` используется правильный API ключ
- Текущий ключ: `AIzaSyAH6yQWf32M41oLhscegD8HTvrGlbjgPRU`

#### 3. **Проверь разрешения Android**
- Настройки → Приложения → Водовозка → Уведомления → Включено
- Батарея → Оптимизация → Водовозка → Не оптимизировать

#### 4. **Проверь сетевое подключение**
- Приложение должно иметь доступ к интернету
- Firebase должен быть доступен

## 🛠️ Альтернативное решение

Если проблема остается, можно запустить локальный push-сервер:

### 1. Установи зависимости
```bash
npm install express firebase-admin cors
```

### 2. Запусти сервер
```bash
# Запусти bat-файл:
start-push-server.bat

# Или вручную:
node push-server-simple.js
```

### 3. Измени код
В `useOrders.ts` раскомментируй блок с localhost:3001

## 📊 Статус исправления

- ✅ **Код исправлен** - логика отправки обновлена
- ✅ **APK пересобран** - новая версия готова
- ✅ **Firebase настроен** - API ключ добавлен
- ⏳ **Тестирование** - требует проверки на устройстве

## 🎯 Следующие шаги

1. **Установи обновленный APK**
2. **Протестируй полный цикл заказа**
3. **Проверь приход уведомлений**
4. **Сообщи о результатах**

---

**Обновлено**: 23.11.2025 18:00  
**APK готов**: `android/app/build/outputs/apk/debug/app-debug.apk`