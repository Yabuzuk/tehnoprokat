# Водовозка - Приложение для заказа услуг

Современное React-приложение для заказа услуг доставки воды и откачки септика.

## 🚀 Технологии

- **Frontend**: React 18, TypeScript, Vite
- **Стилизация**: Tailwind CSS
- **Роутинг**: React Router
- **Состояние**: Zustand
- **API**: React Query
- **Backend**: Supabase (PostgreSQL + Realtime)
- **PWA**: Service Worker, Workbox
- **Mobile**: Capacitor (Android/iOS)

## 📱 Функциональность

### Для пользователей
- 💧 Заказ доставки воды (1300₽/куб.м)
- 🚽 Заказ откачки септика (4000₽)
- Выбор адреса и времени доставки
- История заказов
- Realtime обновления статуса

### Для водителей
- Просмотр доступных заказов
- Принятие/отклонение заказов
- Статистика заработка (90% водителю)
- CRM с фильтрами

### Для админов
- Управление водителями
- Просмотр всех заказов
- Статистика системы

## 🛠 Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd tehnoprokat
```

2. **Установка зависимостей**
```bash
npm install
```

3. **Настройка переменных окружения**
```bash
cp .env.example .env
```
Заполните переменные Supabase в `.env`:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

4. **Настройка базы данных**
- Создайте проект в [Supabase](https://supabase.com)
- Выполните SQL из файла `supabase-schema.sql` в SQL Editor

5. **Запуск в режиме разработки**
```bash
npm run dev
```

6. **Сборка для продакшена**
```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   └── ui/             # UI компоненты (Button, Input, Card)
├── pages/              # Страницы приложения
│   ├── auth/           # Страницы авторизации
│   ├── user/           # Страницы пользователей
│   ├── driver/         # Страницы водителей
│   └── admin/          # Страницы админов
├── hooks/              # Кастомные хуки (React Query)
├── stores/             # Zustand стейты
├── services/           # API сервисы (Supabase)
├── types/              # TypeScript типы
├── utils/              # Утилиты и константы
└── styles/             # Глобальные стили
```

## 🎯 Основные маршруты

- `/` - Выбор роли (пользователь/водитель/админ)
- `/auth/user` - Авторизация пользователя
- `/auth/driver` - Авторизация/регистрация водителя
- `/user/dashboard` - Дашборд пользователя
- `/user/order/:serviceType` - Создание заказа
- `/driver/dashboard` - Дашборд водителя

## 💾 База данных

### Таблицы:
- `users` - Пользователи системы
- `drivers` - Водители с модерацией
- `orders` - Заказы с статусами

### Статусы заказов:
- `pending` - Ожидает принятия
- `accepted` - Принят водителем
- `in_progress` - В процессе выполнения
- `completed` - Выполнен
- `cancelled` - Отменен

## 🔧 Скрипты

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предпросмотр сборки
- `npm run lint` - Проверка кода ESLint
- `npm run test` - Запуск тестов

## 🌟 Особенности

- **Mobile-first дизайн** - Оптимизировано для мобильных устройств
- **PWA** - Можно установить как приложение
- **Realtime** - Обновления в реальном времени через Supabase
- **TypeScript** - Строгая типизация
- **Responsive** - Адаптивный дизайн для всех устройств
- **Dark mode** - Поддержка темной темы

## 📱 PWA

Приложение поддерживает установку как PWA:
- Офлайн режим с Service Worker
- Кэширование API запросов
- Иконки для разных устройств
- Манифест для установки

## 📱 Мобильное приложение (Android)

Приложение также доступно как нативное Android приложение через Capacitor:

### Сборка APK
```bash
# Сборка веб-версии
npm run build

# Копирование файлов в Android проект
npx cap copy android

# Синхронизация плагинов
npx cap sync android

# Сборка APK
cd android && gradlew assembleDebug
```

### Настройка Push-уведомлений
Для работы push-уведомлений необходимо:
1. Настроить Firebase проект
2. Добавить `google-services.json` в `android/app/`
3. Подробные инструкции в файле `PUSH_NOTIFICATIONS_SETUP.md`

### Особенности мобильной версии
- **Автосохранение сессии** - пользователь остается авторизованным
- **Геолокация** - автоматическое определение местоположения
- **Push-уведомления** - уведомления о статусе заказов
- **Офлайн режим** - базовая функциональность без интернета
- **Нативная производительность** - быстрая работа на устройстве

### Готовый APK
Готовый APK файл находится в: `android/app/build/outputs/apk/debug/app-debug.apk` (~7.9 МБ)

## 🚀 Деплой

### Vercel/Netlify
1. Подключите репозиторий
2. Установите переменные окружения
3. Деплой произойдет автоматически

### Docker
```bash
docker build -t water-delivery-app .
docker run -p 3000:3000 water-delivery-app
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте Issue в репозитории.