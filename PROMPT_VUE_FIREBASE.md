# Промпт для создания приложения "Водовозка" на Vue + Firebase

Создай мобильное приложение для заказа услуг доставки воды и откачки септика на **Vue 3 + Firebase**.

## 🎯 Технологический стек

**Frontend:**
- Vue 3 (Composition API, script setup)
- TypeScript
- Vite
- Tailwind CSS
- Vue Router
- Pinia (state management)
- VueUse (composables)

**Backend:**
- Firebase Firestore (база данных)
- Firebase Authentication (авторизация по телефону)
- Firebase Cloud Functions (серверная логика)
- Firebase Cloud Messaging (push-уведомления)
- Firebase Hosting (деплой)

**Mobile:**
- Capacitor (Android/iOS)
- @capacitor/push-notifications
- @capacitor/geolocation

## 📱 Функциональность

### 3 роли пользователей:

**1. Пользователь (клиент):**
- Авторизация по номеру телефона (без пароля, просто имя + телефон)
- Выбор услуги: доставка воды (1300₽/куб.м) или откачка септика (4000₽)
- Ввод адреса доставки с автокомплитом (Yandex Maps API)
- Выбор даты и времени доставки
- Указание количества кубометров (для воды)
- История заказов с фильтрами
- Realtime обновления статуса заказа
- Push-уведомления о смене статуса

**2. Водитель:**
- Регистрация с модерацией (ФИО, телефон, номер машины, тип услуг)
- Просмотр доступных заказов (только своего типа услуг)
- Принятие/отклонение заказов
- Изменение статуса заказа (в пути, выполнен)
- Статистика заработка (90% водителю, 10% сервису)
- Push-уведомления о новых заказах
- CRM с фильтрами по дате, статусу

**3. Администратор:**
- Просмотр всех заказов
- Управление водителями (модерация, блокировка)
- Статистика системы (заказы, выручка)
- Фильтры и поиск

## 🗄️ Структура Firestore

```
users/
  {userId}/
    - name: string
    - phone: string
    - role: 'user'
    - createdAt: timestamp

drivers/
  {driverId}/
    - fullName: string
    - phone: string
    - carNumber: string
    - serviceTypes: ['water_delivery', 'septic_pumping']
    - status: 'pending' | 'active' | 'blocked'
    - createdAt: timestamp

orders/
  {orderId}/
    - serviceType: 'water_delivery' | 'septic_pumping'
    - userId: string
    - driverId: string | null
    - address: string
    - coordinates: { lat: number, lng: number }
    - deliveryDate: string
    - deliveryTime: string
    - quantity: number (для воды)
    - price: number
    - status: 'pending' | 'accepted' | 'in_progress' | 'completed' | 'cancelled'
    - createdAt: timestamp
    - updatedAt: timestamp

pushTokens/
  {tokenId}/
    - token: string
    - userId: string | null
    - driverId: string | null
    - deviceType: 'android' | 'ios'
    - updatedAt: timestamp
```

## 🔥 Firebase Cloud Functions

**1. onOrderCreate** - при создании заказа отправить push всем водителям нужного типа
**2. onOrderStatusChange** - при смене статуса отправить push пользователю
**3. sendPushNotification** - HTTP функция для отправки push через FCM

## 🎨 UI/UX требования

- Mobile-first дизайн
- Минималистичный интерфейс
- Цветовая схема: синий (#3b82f6) для воды, зеленый (#10b981) для септика
- Адаптивность для всех экранов
- Плавные анимации (Vue transitions)
- Skeleton loaders при загрузке
- Toast уведомления (vue-toastification)

## 📁 Структура проекта

```
src/
├── components/
│   ├── ui/              # Button, Input, Card, Modal
│   └── common/          # Header, Footer, Loading
├── views/
│   ├── auth/            # Login, Register
│   ├── user/            # Dashboard, CreateOrder, Orders
│   ├── driver/          # Dashboard, OrdersList, Stats
│   └── admin/           # Dashboard, Drivers, Orders
├── composables/         # useAuth, useOrders, useNotifications
├── stores/              # auth.ts, orders.ts (Pinia)
├── services/            # firebase.ts, api.ts
├── types/               # index.ts (TypeScript interfaces)
├── utils/               # constants.ts, helpers.ts
└── router/              # index.ts
```

## 🚀 Маршруты

```
/ - выбор роли
/auth/user - авторизация пользователя
/auth/driver - регистрация водителя
/user/dashboard - дашборд пользователя
/user/order/:type - создание заказа
/user/orders - история заказов
/driver/dashboard - дашборд водителя
/driver/orders - список заказов
/admin/dashboard - админ панель
```

## 🔔 Push-уведомления

- Регистрация токена при входе
- Сохранение в Firestore
- Отправка через Cloud Functions
- Обработка в фоне (Capacitor)
- Локальные уведомления при открытом приложении

## 📱 Capacitor интеграция

- Конфигурация для Android/iOS
- Push Notifications плагин
- Geolocation для автоопределения адреса
- SplashScreen
- StatusBar

## 🔐 Безопасность

- Firestore Security Rules для каждой коллекции
- Валидация данных на клиенте и сервере
- Защита Cloud Functions (только авторизованные)
- Скрытие чувствительных данных

## 📦 Дополнительно

- PWA поддержка (manifest, service worker)
- Офлайн режим (кэширование)
- Оптимизация bundle size
- Lazy loading компонентов
- Error boundary
- Логирование ошибок

## 🎯 Результат

Полностью рабочее приложение с:
- ✅ Авторизацией по телефону
- ✅ Созданием и управлением заказами
- ✅ Realtime обновлениями
- ✅ Push-уведомлениями (фоновыми)
- ✅ Мобильным APK для Android
- ✅ Готовым к деплою на Firebase Hosting

Создай полную структуру проекта с рабочим кодом, Firebase конфигурацией, Cloud Functions и инструкциями по запуску.