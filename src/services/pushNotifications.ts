import { supabase } from './supabase'

export const pushNotifications = {
  // Уведомления теперь отправляются автоматически через триггеры БД
  // Эти функции оставлены для совместимости, но можно удалить
  
  async notifyDriversNewOrder(orderId: string, serviceType: string, address: string) {
    console.log('📤 Уведомления водителям отправляются автоматически через триггер БД')
    // Триггер автоматически отправит уведомления при создании заказа
  },

  async notifyUserStatusChange(userId: string, orderId: string, status: string) {
    console.log('📤 Уведомления пользователям отправляются автоматически через триггер БД')
    // Триггер автоматически отправит уведомления при изменении статуса
  }
}
