# 🔥 Настройка Firebase для Push-уведомлений

## Шаг 1: Получить Service Account Key

### 1.1 Откройте Firebase Console
Перейдите на: https://console.firebase.google.com/

### 1.2 Выберите проект
Найдите проект `newagent-c434a`

### 1.3 Перейдите в настройки
1. Нажмите на шестеренку ⚙️ (Settings)
2. Выберите "Project settings"

### 1.4 Откройте вкладку "Service accounts"
1. Нажмите на вкладку "Service accounts"
2. Выберите "Firebase Admin SDK"

### 1.5 Создайте новый ключ
1. Нажмите "Generate new private key"
2. Подтвердите действие
3. Скачается файл `service-account-key.json`

### 1.6 Поместите файл в проект
Скопируйте `service-account-key.json` в корень проекта `e:/tehnoprokat/`

## Шаг 2: Активировать Firebase Admin SDK

### 2.1 Раскомментируйте код в push-server.cjs
Откройте файл `push-server.cjs` и раскомментируйте строки:
```javascript
// Убрать комментарии с этих строк:
initializeApp({
  credential: cert('./service-account-key.json')
});
```

### 2.2 Заменить заглушки на реальные вызовы
В функциях отправки уведомлений заменить заглушки на реальные вызовы Firebase Admin SDK.

## Шаг 3: Запустить сервер
```bash
node push-server.cjs
```

## ⚠️ Безопасность
- НЕ добавляйте `service-account-key.json` в Git
- Добавьте файл в `.gitignore`
- В продакшене используйте переменные окружения

## 🧪 Тестирование
После запуска сервера можно тестировать:
```bash
curl -X POST http://localhost:3001/send-notification \
  -H "Content-Type: application/json" \
  -d '{
    "token": "ваш-push-token",
    "title": "Тест",
    "body": "Тестовое уведомление"
  }'
```